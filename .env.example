# Copy to .env for local development (expo start reads this automatically).
# EAS builds do NOT use this file — they get API_BASE_URL from the build
# profile's `env` block in eas.json instead. See CLAUDE.md §3 for the
# per-client onboarding process.
API_BASE_URL=https://func-ordermanager-nilambur.azurewebsites.net
